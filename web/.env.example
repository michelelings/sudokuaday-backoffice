# Parity ingest (local / CI shell — not prefixed with VITE_)
# SUDOKUADAY_REPO_PATH=/path/to/sudokuaday.com
# SUDOKUADAY_REPO_SHA=optional
# STALE_LAG_HOURS=24

# Optional backoffice gate
# VITE_BACKOFFICE_PASSWORD=

# Future: analytics connectors will use a server/BFF or CI job — not Vite env vars.
# Do not put GA/GSC/Ahrefs/Semrush secrets in VITE_* (they ship to the browser).
